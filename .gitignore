# Documentation files

# Data directory — ignore downloads and generated packs; keep the build-time placeholder
data/*.jsonl
data/*.jsonl.gz
data/*.bin.gz
!data/en.bin.gz
data/*.version

# Binary
lexify

docs/private/